claude-chai — apply these changes
=================================

Two ways to land them in your claude-chai repo:

A) COPY FILES — extract this zip into the repo root, preserving paths
   (it creates/overwrites the listed files), then: git add -A && git commit && git push

B) GIT PATCH (preserves the 3 commits + authorship) — use the included .patch:
     git checkout claude/amazing-wright-8vsuns      # or a new branch off main
     git am < claude-chai-changes.patch
     git push -u origin claude/amazing-wright-8vsuns

ONE MANUAL STEP — the mascot:
   Save the green-devil image you provided as:  assets/claude-chai.png
   (README.md references it there. assets/ already contains a placeholder note.)
